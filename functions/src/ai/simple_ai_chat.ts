// simple_ai_chat.ts legacy handler removed — placeholder file

// Use `functions/src/aiChatHttp.ts` which contains the consolidated and maintained
// ai HTTP endpoint. This stub exists so the codebase doesn't reference legacy logic.

export {};
